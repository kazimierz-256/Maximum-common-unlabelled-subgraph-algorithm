using GraphDataStructure;
using System;
using System.Collections.Generic;
using System.Text;

namespace SubgraphIsomorphismExactAlgorithm
{
    public class ScoreUpperLimit
    {
        List<Graph> GetAllGraphWithScoreAbove(double score, Graph g)
        {
            // recursively discover all graphs until encountered one with score of @score
            // gather all those graphs based on their hashes into a dictionary (of score & hash)
            // In the Graph class add ability to compute hashes within @k iterations
            return null;
        }
    }
}
